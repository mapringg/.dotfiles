# Code Style

## Comments

- Use single-line `//` comments instead of JSDoc `/** */` blocks
- Sentence case (capitalize first word only)
- No trailing punctuation
- Short, descriptive section headers are fine (e.g., `// Command: /commit`)
- Remove verbose inline explanatory comments that describe what code does
