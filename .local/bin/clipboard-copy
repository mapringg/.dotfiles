#!/usr/bin/env bash
set -euo pipefail

if command -v pbcopy >/dev/null 2>&1; then
  exec pbcopy
elif command -v wl-copy >/dev/null 2>&1; then
  exec wl-copy
else
  echo "clipboard-copy: no supported clipboard command found (pbcopy, wl-copy)" >&2
  exit 1
fi
